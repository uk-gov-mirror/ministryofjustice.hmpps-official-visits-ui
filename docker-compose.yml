version: '3.1'
services:
  redis:
    image: 'redis:8.10'
    networks:
      - hmpps
    container_name: redis-official-visits
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    ports:
      - '6379:6379'

networks:
  hmpps:
